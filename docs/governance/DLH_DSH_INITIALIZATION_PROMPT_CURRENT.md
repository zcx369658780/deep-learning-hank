# DSH 短启动入口

当前科学任务：Issue #75 / DLH-WL-P1B。
路线：`DLH-WL-V1-20260918`。

使用：

```text
接续 zcx369658780/deep-learning-hank，执行 GitHub Issue #75。fresh-fetch origin/main，读取 AGENTS.md、Task Index、路线锁和 #75 full body/comments，确认 final activation、operative baseline 与专用分支。严格按 #75 allowlist 完成数据/标签 schema 与 P2 offline experiment contract 设计；不训练、不下载数据，不跑 pytest/full suite/HJB/KFE/GE/MATLAB。完成后 commit+push 专用 branch，留一次 completion comment并 STOP。
```

聊天 prompt 不扩大 Issue authority。若 live Issue、Task Index、activation 或 baseline 冲突，fail closed。

# Deep Learning + HANK Project Source Manifest

Date: 2026-08-19

Purpose: bootstrap and handoff source for the new clean-slate `zcx369658780/deep-learning-hank` project.

## Included source families

- `project_rules/` — current governance, readonly-reference, diagnostics, evidence and acceptance rules.
- `design_notes/` — clean-slate Python / deep-learning + HANK design thinking and legacy boundary.
- `historical_model_analysis/` — curated Codex analyses from the former multi-province Matlab HANK / R5 route; historical reference only.
- `tasks/` — synchronized task pointer and future DLH-0 outline; active Builder authority is a GitHub Issue, not local task prose.
- `docs/governance/` — GitHub bootstrap authority / current handoff documents.

## Authority boundary

- live GitHub `main` is synchronized repository/governance authority;
- an open GitHub Issue explicitly identified by `tasks/TASK_INDEX_CURRENT.md` is the sole DSH Builder task authority;
- legacy analysis does not carry model/output authority into the new project;
- DSH local reference roots are read-only;
- the new project starts with no model code and no Results authority.

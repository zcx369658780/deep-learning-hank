# DLH-0 推荐首任务轮廓

状态：OUTLINE ONLY / 未激活

目标：冻结新 Deep Learning + HANK 模型的 scientific constitution。

必须决定：

- paper research question；
- exact role of deep learning；
- minimum HANK structure；
- household states/controls；
- regional dimension；
- spatial/exposure mechanisms；
- firm/nominal/fiscal scope；
- shock process；
- steady-state/transition concept；
- benchmark solver/special cases；
- validation metrics；
- Python/PyTorch dependency policy；
- local project path；
- reference import policy；
- first implementation gate。

禁止：写模型代码、运行 Matlab、训练网络、生成 Results。

# DLH-5V-F — Represented-Transition and Moment-Cone Audit (Issue #54)

Companion report 3 of 5. For every deferred W-active state class, compares the
nonnegative first-moment cone of **actual represented native-grid destinations** with
the continuous admissible drift cone, and proves the finite-process representability
obstruction. Exact closed-form arguments plus tiny exact enumeration evidence
(`%TEMP%` spot-checks, never committed).

---

## 1. Destination enumeration rule

From a state `s = (j, i)`, the candidate destination set is

```text
D(s) = { (j', i') : 0 <= j' <= 19, i' >= 0, 10 j' + 7 i' <= N, (j', i') != (j, i) }
```

i.e. **every** represented native-grid node other than the source. The audit does not
assume the accepted transition set `{w_left, w_down, w_T, w_RT}` is exhaustive: any
alternative native-grid displacement is a legitimate candidate (it connects two
actually represented native states; the whole straight segment stays in `D_W` by
convexity because both endpoints are in `D_W`). Ghost/interpolation/virtual states
are excluded by construction (all destinations are real nodes).

## 2. Moment cone

For nonnegative rates `q_d >= 0` on destinations `d in D(s)` with displacement
`v_d = ((10/19) Delta j_d, (7/19) Delta i_d)`,

```text
C_rep(s) = { sum_d q_d v_d : q_d >= 0 }
```

The continuous admissible cone at a `{W}`-only state is `T_W = { mu_W <= 0 }`, where
`mu_W = mu_a + mu_b` and `Delta W` of a destination satisfies

```text
Delta W_d = (10 Delta j_d + 7 Delta i_d)/19.
```

## 3. Key lemmas

**Lemma 1 (W-budget of a destination).** For a top cell with residual `r_j`, the
source W-index is `W_idx(s) = N - r_j`; a destination has
`10 Delta j + 7 Delta i = W_idx(dest) - W_idx(s) <= r_j`. For an **exact-frontier top
cell (`r_j = 0`)**: `10 Delta j + 7 Delta i <= 0` for **every** represented
destination, with equality iff `dest` is same-W (`W_idx(dest) = N`).

*Proof.* `W_idx(dest) <= N` by the node set; subtract `W_idx(s) = N - r_j`. For
`r_j = 0` the upper bound is `0`.

**Lemma 2 (same-W generation).** `10 Delta j + 7 Delta i = 0` iff
`(Delta j, Delta i) = k (7, -10)`, `k in Z` (gcd(10,7) = 1). The primitive rays are
the mirror `(+7,-10)` (`k = 1`, available iff `j <= 12` and `i >= 10`) and the
forward `(-7,+10)` (`k = -1`, available iff `j >= 7`).

**Lemma 3 (sliding-ray membership at exact-frontier cells).** At a cell with
`r_j = 0`, any combination with `mu_W = 0` has positive mass only on same-W
destinations (Lemma 1: every non-same-W destination has strictly negative
`Delta W`). Hence `C_rep ∩ { mu_W = 0 }` is the cone generated by the **available**
same-W rays.

## 4. Obstruction certificate — exact-frontier top cells (`r_j = 0`)

**Lower band `j in {1..6}` (a-interior, b-interior, active set `{W}`).** The state is
an exact-frontier top cell: `W_idx = N`. Same-W rays available: mirror rays only
(`j + 7 <= 13 <= 19`, `i - 10 >= 9` — available; forward `j - 7 < 0` — off-grid),
all with `Delta b < 0`, `Delta a > 0`. By Lemma 3,

```text
C_rep(s) ∩ { mu_W = 0 } = { (mu_a, mu_b) : mu_a >= 0, mu_b = -mu_a <= 0 }
```

(cone of the mirror ray). The **forward-sliding ray** `{(-u, +u), u > 0}` has
`mu_W = 0`, `mu_b = +u > 0` and is continuously admissible (only `W` active
`=> T_W`), but `(-u,+u) ∉ C_rep(s)`. **Obstruction (O_L).**

**Upper band `j in {13..18}` (a-interior, b-interior, active set `{W}`).** Same-W
rays available: forward rays only (`j >= 13 >= 7`; mirror `j + 7 > 19` off-grid),
all with `Delta a < 0`, `Delta b > 0`:

```text
C_rep(s) ∩ { mu_W = 0 } = { (mu_a, mu_b) : mu_a <= 0, mu_b = -mu_a >= 0 }.
```

The **reverse-sliding ray** `{(+u, -u), u > 0}` (`mu_W = 0`, `mu_a = +u > 0`,
admissible) satisfies `(+u,-u) ∉ C_rep(s)`. **Obstruction (O_U).**

These two certificates are exact and airtight: they use only (i) the node-set bound,
(ii) same-W generation, (iii) the availability conditions — no enumeration was
needed.

## 5. Obstruction certificate — additional no-helper classes (dominance)

**Lower band `j = 1`, `r_1 in {1,2,3}` (top cell).** Exact enumeration (N in
[190,260]) establishes the certificate property:

```text
(P_L)  no represented destination has (Delta b > 0 AND 10 Delta j + 7 Delta i >= 0).
```

At these cells the only `Delta b > 0` destination is `(0, i+1)` (displacement
`(-1, +1)`, `Delta W = -3/19`); every `Delta W > 0` destination has
`Delta a > 0` and `Delta b < 0` with b-drag per unit W at least `7/3` (exact
minimum over the enumerated `Delta W > 0` destinations: ratios `{7/3, 14, 49, ...}`;
for `r_1 = 1` the minimum is 49, for `r_1 = 2` it is 14, for `r_1 = 3` it is `7/3`).
If `(-u, +u) = sum q_d v_d` then `mu_W = 0` requires the `Delta W < 0` mass of
`(0, i+1)` (at least `3 q` in index units) to be balanced by `Delta W > 0` mass,
whose b-drag is at least `(7/3) * 3 q = 7 q` — exactly cancelling the `+7 q` b-gain
of `(0, i+1)`; the equality-degenerate case additionally has `mu_a > 0` (wrong
sign). Hence `mu_b > 0` with `mu_W = 0` is impossible: `(-u, +u) ∉ C_rep(s)`.
**Obstruction (O_L).**

**Upper band no-helper cells `j in {13..18}`, `r_j in {1,2}`** (per-residue subset,
Section 8). The symmetric certificate holds:

```text
(P_U)  no represented destination has (Delta a > 0 AND 10 Delta j + 7 Delta i >= 0),
```

with the analogous dominance bound (a-gain of `Delta a > 0` mass is cancelled by the
a-drag of the balancing `Delta W > 0` mass, whose worst ratio is again bounded away
from covering it). Hence the reverse-sliding ray `(+u, -u) ∉ C_rep(s)`.
**Obstruction (O_U).**

The dominance bounds are stated as exact inequalities; the per-residue cell lists and
the property checks `P_L` / `P_U` were machine-verified by exact enumeration over
`N in [190, 260]` (Section 8).

## 6. Representable band cells (constructive exact-tangent coverage)

For completeness: at the remaining band cells the exact W-tangent sliding ray IS in
`C_rep(s)` via alternative native-grid destinations (all actual nodes):

- Top cells `j in {1..6}` with `r_j >= 4`: forward-sliding via
  `(j-1, i+2)` (displacement `(-10/19, +14/19)`, `Delta W = +4/19`) and
  `(j, i-1)` (`Delta W = -7/19`):

  ```text
  q_1 = 19 u / 10  on (j-1, i+2),   q_2 = 38 u / 35  on (j, i-1)
  =>  sum = (-u, +u) exactly   (verified: mu_W = (4 q_1 - 7 q_2)/19 = 0)
  ```

- Top cells `j in {2..6}` with `r_j in {1,2,3}`: forward-sliding via `(j-2, i+3)`
  (`Delta W = +1/19`) plus `(j, i-1)` (`q_1 = 7 q_2` scaling, exact `(-u, +u)`).
- Upper-band cells with `r_j = 3` (and `i >= 2`): reverse-sliding via `(j+1, i-1)`
  (`Delta W = +3/19`, `Delta a > 0`) plus `(j+1, i-2)` (`Delta W = -4/19`):
  `q_1 = 4 k`, `q_2 = 3 k`, `k = 19 u / 70` gives `(+u, -u)` with `mu_W = 0`
  exactly.
- Every sub-top band cell: `(j-1, i+2)` is always represented
  (`W_idx = N - r_j - 3 <= N`), giving the forward helper; `(j+1, i-1)` is always
  represented (`W_idx = N - r_j - 4`), giving the reverse helper; sliding coverage
  follows by the same two-destination constructions.

These decompositions are exact (Fraction-verified) and are constructive evidence
that the obstruction is confined to the classes of Sections 4–5; they are not
claimed to be full closed-form contracts for arbitrary admissible drift.

## 7. Closable classes — cone equality (exact)

For the economic-face deferred classes the full continuous admissible cone equals
the accepted sector cone:

```text
j = 0 (a=0 x W):    admissible {mu_a >= 0, mu_W <= 0}
                    = cone{w_RT, w_down}            (reverse sector; mu_b <= 0 forced)
j = 19 (a_max x W): admissible {mu_a <= 0, mu_W <= 0}
                    = cone{w_left, w_down, w_T}     (T_realloc ∪ R_deplete)
(19,0) (b_min x W): admissible {mu_a <= 0, mu_b >= 0}
                    = cone{w_left, w_T}             (T_realloc; mu_W <= 0 automatic)
```

In each case the economic-face multiplier cuts off exactly the drift region that the
missing wide stencil would serve (`mu_a < 0` at `a = 0`; `mu_a > 0` at `a = a_max`;
`mu_b < 0` at `b = b_min`), so the closure is exact with rates identical to the
regular formulas (seam-consistent: `j=0 <-> j=7` reverse, `j=12 <-> j=19` forward,
b-min face T_realloc).

## 8. Exact enumeration evidence (tiny spot-checks, `%TEMP%`, not committed)

Exact integer/fraction enumeration over `N in [190, 260]` (all seven residues mod 7),
all deferred top/sub-top cells of classes `j in {0..6}`, `j in {13..19}`, `i <= 9`:

- **Certificate properties `P_L`/`P_U`** (no same-orientation `Delta W >= 0`
  destination) hold exactly on the obstruction cells listed below, and fail on every
  other band cell (i.e. the obstruction set is exactly the no-helper set, verified
  cell-by-cell).
- **Universality:** every residue `N mod 7` has at least one obstruction cell:

```text
residue 0: upper band j = 14 (r=0)  [plus no-helper (16,1),(18,2) etc.]
residue 1: lower band j = 5 (r=0)
residue 2: lower band j = 3 (r=0);  upper band j = 17 (r=0)
residue 3: lower band j = 1 (r=0);  upper band j = 15 (r=0), 17 (r=1)
residue 4: lower band j = 1 (r=1), 6 (r=0);  upper band j = 13 (r=0), 15 (r=1), 17 (r=2)
residue 5: lower band j = 1 (r=2), 4 (r=0);  upper band j = 18 (r=0)
residue 6: lower band j = 1 (r=3), 2 (r=0);  upper band j = 16 (r=0), 18 (r=1)
```

  (`r = 0` cells alone already cover every residue; the `r in {1,2}` no-helper cells
  are additional.)
- **Exact decompositions** of Section 6 verified with `Fraction` arithmetic
  (moments exactly `(-u, +u)` / `(+u, -u)` with `mu_W = 0`).
- **Cone equalities** of Section 7 verified algebraically (accepted sector formulas).

## 9. Which frozen assumption causes the obstruction

The frozen grid/domain places the primitive same-W destination of the missing
orientation off-grid in the a-interior endpoint bands:

```text
forward (j-7, i+10) requires j >= 7  -> off-grid for j in {1..6}   (a_dest < 0)
mirror  (j+7, i-10) requires j <= 12 and i >= 10 -> off-grid for j in {13..18} (a_dest > a_max)
```

while the frozen exact same-process contract (i) admits only actual represented
native-grid destinations (no ghost/interpolation/virtual states, no reflected mass),
and (ii) requires exact first-moment equality for tangent motion (`mu_W = 0`; no
inward-normal injection to disguise the mismatch — explicitly forbidden by Issue #54
unless the Owner later changes the approximation contract). At an exact-frontier cell
every non-same-W destination strictly decreases `W`, so no nonnegative combination
can realize `mu_W = 0` in the missing orientation. The obstruction is a lattice
(structural) property of the frozen domain/grid, not an algebra or economics error.

## 10. Scope honesty

- The report proves (a) exact closure for the economic-face classes, (b) exact
  obstruction for the Section 4 classes (closed-form, universal) and Section 5
  classes (enumeration-backed dominance), (c) constructive exact-tangent coverage for
  the remaining band cells. It does **not** claim full closed-form candidate
  contracts for arbitrary admissible drift at the Section 6 cells; the terminal is
  already determined by the obstruction.
- No clipping, no omitted destination with retained diagonal escape, no ghost/
  interpolation/virtual state, no KFE-only repair, no second boundary process.

# DLH-5V-H — Authority and Primary-Theory Capsule (Micro-Rev Rev 6)

**Issue:** #56 / DLH-5V-H
**Task type:** `SCIENTIFIC_THEORY_AUDIT__STATE_CONSTRAINT_HJB_BOUNDARY_CONSISTENCY_TARGET`
**Branch:** `dsh/issue-56-dlh-5vh-state-constraint-consistency-audit-2026-09-12`
**Date:** 2026-09-12 — **Micro-Rev Rev 6** on top of Rev 5 candidate `82edcbb7d1438fc108a5c17605414efda627b535` (itself on top of `e8d49fe9d02fbb5961bc1bf7076a4b2d93e767fb` ← `dd3aa425a869a2d5a09835efc667c4ff1c2bb5b3` ← `52cfbf4cb4a1bcdb36bd77211869e8ac0f51b317` ← `5d1e4822dcc94aa16d30837e9c524baaefe5fa3d` ← `bc281a693b2d9281b6e180bc62996d503dfab7fe`)
**Reviewer (Rev 5 verdict):** comment `5643802722` — `DLH_5VH_OUTCOME_B_NOT_YET_ACCEPTED__DISCRETE_BELLMAN_COERCIVITY_AND_STABILITY_WITH_UNBOUNDED_CONTROLS_FIX_REQUIRED`

---

## Digest (Rev 6)

1. DLH-5V-H audits whether the accepted Issue-#55 raw admissible-drift-set graph target is mathematically necessary for state-constraint HJB scheme convergence. Rev 6 keeps the Rev-5 parameter-generic continuous audit and fixes the DISCRETE-LAYER defect: the continuous-Hamiltonian optimizer compactness (K_alpha(K_p)) does NOT by itself localize the finite-m discrete Bellman maximizers; a discrete smooth-test coercivity / optimizer-localization lemma is proved; the invalid raw-domain stability and strict-contraction claims are deleted/demoted; the Outcome-B block is renamed.
2. **Parameter authority (kept, controlling):** the immutable `EconomicParams` source freezes ONLY `γ_c > 0` and `φ > 0` (validation lines 45–46); the repository instances `EconomicParams(0.02, 2.0, 5.0, 0.1, 2.0, 1e-6, 0.0, 0.0)` (in `tests/test_dlh_4b_transfer.py`, `tests/test_dlh_4d_r3_transfer_foc_parity.py`, `tests/test_dlh_5k_high_wealth_corner_closure_diagnostic.py`, `tests/test_dlh_5l_total_wealth_domain_geometry_diagnostic.py`) are explicitly `VALIDATION_FIXTURE_NOT_CALIBRATION` and are NOT promoted to theory authority. All statements are parameter-generic.
3. **TRUE control domain (kept):** c > 0, l ≥ 0, d ∈ R with the source-exact drift μ_b = r_b·b + labor_income − d − χ(d,a) − c; no static budget (μ_b ≥ 0 is a b_min tangent law only); τ is the wage wedge inside effective wages, not additive resources; no installed hard l/d bounds; sup (not max) until attainment is proved.
4. **Hamiltonian effective domain — PARAMETER-GENERIC (kept, the theorem):** with u(c) = log(c) for γ_c = 1, else c^{1−γ_c}/(1−γ_c), γ_c > 0: p_b > 0 finite+attained for EVERY γ_c > 0, φ > 0; p_b < 0 +∞ from +|p_b|·χ_1 d²/(2s); p_b = 0, p_a ≠ 0 +∞; p_b = p_a = 0: γ_c > 1 ⟹ 0 (not attained), γ_c = 1 ⟹ +∞, 0 < γ_c < 1 ⟹ +∞. Hence `0 < γ_c ≤ 1 ⟹ finite ⟺ p_b > 0; γ_c > 1 ⟹ finite ⟺ p_b > 0 or (p_b = 0, p_a = 0)` — the γ_c > 1 branch conditional on the parameter regime.
5. **Continuous local effective compactness / coercivity (PROVED, kept — parameter-generic):** on compact K_p ⊂ {p_b ≥ η > 0} (|p_a| ≤ P, η ≤ p_b ≤ P): P^{−1/γ_c} ≤ c* = p_b^{−1/γ_c} ≤ η^{−1/γ_c}; l_j* ≤ (P·max w·z/ω_min)^{1/φ}; |d*| ≤ (s/χ_1)(2P/η + χ_0); common compact K_alpha(K_p); sup = max there; |μ| ≤ M.
6. **Hamiltonian regularity on the effective region (PROVED, kept):** H_proj jointly continuous in (x, p) (maximum over a fixed compact control set / compact-maximum (Berge) argument; K_alpha is generally a continuum), Lipschitz in p. **The uniform second moment O(1/m) over the CONTINUOUS optimizer set K_alpha(K_p) is PROVED but does NOT localize the discrete maximizers** — that is the Rev-6 corrected logical order.
7. **DISCRETE smooth-test coercivity / optimizer-localization lemma (PROVED — Rev 6, route A):** for every compact (N, K_p ⊂ {p_b ≥ η > 0}) and every smooth test φ with locally bounded Hessian, for all m ≥ m₀(N, K_p, C_φ, geometry) the exact/ε-maximizers of the finite-m discrete Bellman objective Ψ_m(α) = g + Σ q[φ(x_r)−φ(x_s)] lie in ONE common compact control set K_alpha^disc independent of m ≥ m₀; the finite-m discrete max is ATTAINED; |μ| ≤ M and the uniform second moment O(1/m) hold THROUGH the actual max; the Taylor remainder passes through the max only after this localization. Logical order: raw unbounded controls → discrete coercivity/localization → compact maximizing controls → uniform drift/rate bound → uniform second moment → max-level Taylor consistency. The continuous K_alpha(K_p) is used only as the uniform lower anchor (a continuous maximizer α* exists) and for H_proj regularity.
8. **Scheme-level stability / strict contraction (Rev-6 demotion):** the old `‖u_m‖∞ ≤ ‖g‖∞/ρ` is INVALID on the raw domain (‖g‖∞ = ∞: g unbounded below in l for every φ > 0, unbounded above in c for 0 < γ_c ≤ 1) — DELETED. Per-candidate weight sum λ/(ρ+λ) < 1 does NOT imply a global strict-contraction modulus (λ unbounded on raw controls). Monotonicity / order preservation remains PROVED at the candidate level. Scheme-level existence/stability/unique fixed point is DEMOTED to the Outcome-B block.
9. **Constant-test / operator-finiteness diagnostic (Rev 6):** Q·1 = 0 ⟹ the discrete score on a constant test reduces to g; sup over raw c of g is +∞ for 0 < γ_c ≤ 1 ⟹ the Bellman operator is not globally finite on arbitrary bounded functions under the raw control space; economically relevant solutions may live in the p_b > 0 effective region; global bounded-function contraction cannot be assumed; BS/Soner mapping needs an effective-domain/generalized treatment — recorded inside the block.
10. **Local vs global η (kept):** at a FIXED test gradient with p_b > 0 a local η < p_b exists — LOCAL OPERATOR CONSISTENCY PROVED there; a GLOBAL uniform η is demanded only if the selected comparison theorem requires it. Preferred unresolved question: do all viscosity gradients used by the state-constraint comparison/consistency argument remain inside the finite effective Hamiltonian domain, and where needed away from p_b = 0?
11. **Boundary transfer (Rev-6 status):** H_m ≤ H_proj trivial; the full discrete-to-continuous Taylor passage through the Bellman max is justified by the §D.5 localization; the transfer is **PROVED for every effective-domain smooth-test gradient (p_b > 0, locally bounded Hessian)**; overall **CONDITIONAL_ON_EFFECTIVE_DOMAIN_REGULARITY**.
12. **Soner assumption mapping (kept, explicitly not assumed):** Soner Part I assumes a COMPACT control metric space and bounded drift/running cost; raw project controls are not compact — whether the local effective compactification on {p_b > 0} suffices to map the whole problem into the theorem, or an extension/generalization is required, must be resolved — do not cite Soner as directly applicable until resolved.
13. **Tangent recovery (kept, parameter-independent):** all strict and all tangent controls at every family remain PROVED against the true control domain; exact-arithmetic verified. Rev-1–4 repairs preserved (Soner sign mapping; project convention; H_T downgrade; corrected monotonicity; frozen finite-m taxonomy; μ_W = 1; quantifier-corrected half-relaxed-limit; exact-χ algebra; mirror wording j+7 ≤ 19m, i ≥ 10).
14. **Ledger (matches the evidence):** PROVED — sign mapping; raw-graph non-necessity mechanism; monotonicity/order preservation (candidate level); interior consistency; stencil destination availability; finite-m rate algebra; local-state same-candidate recovery (all families, parameter-independent); TRUE Hamiltonian effective domain (parameter-generic); continuous local effective compactness/coercivity on {p_b ≥ η > 0}; H_proj joint continuity/USC and Lipschitz in p on the effective region; **discrete smooth-test coercivity / optimizer-localization (§D.5) incl. finite-m discrete max attainment and uniform O(1/m) second moment through the actual max**; boundary smooth-test operator consistency for every effective-domain test gradient (p_b > 0); interior supersolution; composite H_T ≤ liminf H_R ≤ limsup H_R ≤ H_proj. UNRESOLVED (single bounded block — renamed): the UNBOUNDED-CONTROL NUMERICAL-SCHEME / STATE-CONSTRAINT CONVERGENCE-APPLICATION BLOCK — (i) global finite-m stability/existence of discrete solutions, (ii) strict contraction / unique fixed point for production, (iii) constant-test/operator-finiteness diagnostic, (iv) relevant-viscosity-gradient effective-domain restriction, (v) local-effective-compactification → Soner/CDL comparison mapping and exact theorem application, (vi) optional constrained characterization ρV ≤ H_T. **Outcome B** (Outcome C NOT triggered: the frozen process defines a finite legitimate discrete score on the p_b > 0 smooth-test class).

**Terminal (exactly one, Rev 6):**

`DLH_5VH_THEORY_AUDIT_PARTIAL__ONE_BOUNDED_BOUNDARY_CONSISTENCY_OR_COMPARISON_GAP_REMAINS`

---

## Authority

- Live `main` at activation-refresh and at Reviewer checks: `5d38c644ec1a06359241f9c3418dcd815c938ec6` (re-verified by fresh fetch; unchanged; Issue #56 remains OPEN).
- Activation comments: `5641527846` (initial activation), `5641534701` (activation refresh). Reviewer comments: `5641997550` (Rev-0 verdict), `5642335215` (Rev-1 verdict), `5642677284` (Rev-2 verdict), `5642868947` (Rev-3 verdict), `5643574257` (Rev-4 verdict), `5643802722` (Rev-5 verdict). Prior completion comments: `5642280244` (Rev 1), `5642519208` (Rev 2), `5642806537` (Rev 3), `5642954533` (Rev 4), `5643749417` (Rev 5).
- Owner route decision: `APPROVE_DLH_5VH_ROUTE_E_STATE_CONSTRAINT_HJB_CONSISTENCY_TARGET_AUDIT`.
- Accepted household source (read-only, blob verified): `src/deep_learning_hank/two_asset/matlab_faithful_two_asset_ha.py` = `76ae5b149993a7edeeb8eb337f1b02b3fe33c51e`.
- Consumed, not reopened: Issue #54 (acceptance `5633995486`, integration `4e77d9c7…`), Issue #55 Outcome C (acceptance `5635802416`, integration `8dd5e9c4…`).
- Previous candidates: Rev 0 `bc281a693b2d9281b6e180bc62996d503dfab7fe` (Outcome A, NOT accepted); Rev 1 `5d1e4822dcc94aa16d30837e9c524baaefe5fa3d` (Outcome B, NOT accepted); Rev 2 `52cfbf4cb4a1bcdb36bd77211869e8ac0f51b317` (Outcome B, NOT accepted); Rev 3 `dd3aa425a869a2d5a09835efc667c4ff1c2bb5b3` (Outcome B, NOT accepted — false global budget + effective domain); Rev 4 `e8d49fe9d02fbb5961bc1bf7076a4b2d93e767fb` (Outcome B, NOT accepted — fixture parameters promoted to theory authority); Rev 5 `82edcbb7d1438fc108a5c17605414efda627b535` (Outcome B, NOT accepted — discrete Bellman coercivity/stability with unbounded controls). This Rev 6 amends on the same branch, same six-file allowlist.

---

## Primary-theory provenance

Cited with metadata only (no copyrighted PDFs, no long quotations). Statements are labeled PRIMARY THEOREM / PROJECT-SPECIFIC DERIVATION / PLAUSIBLE-BUT-NOT-YET-PROVED.

| Reference | DOI / metadata | Role in this audit | Status label |
|---|---|---|---|
| H. M. Soner (1986), "Optimal Control with State-Space Constraint I" | SIAM J. Control Optim. 24(3):552–561, DOI `10.1137/0324032` | State-constrained optimal control; minimization convention `H_S(x,p) = sup{−b·p − f}`, residual `G_S = ρv + H_S`; constrained viscosity solution = **subsolution in the open domain / supersolution on the closed domain** (Definition 2.1 / Theorem 2.1 locations verified by the Reviewer, comments `5641997550`, `5642335215`); also the COMPACT-control-space / bounded-drift / bounded-running-cost hypotheses | PRIMARY THEOREM (convention, orientation, and hypotheses as verified by the Reviewer); the project transform is a PROJECT-SPECIFIC DERIVATION (continuous-target report §2.2); the mapping of the project's unbounded-control problem into the theorem is PLAUSIBLE-BUT-NOT-YET-PROVED (the Outcome-B block) |
| H. M. Soner (1986), "Optimal Control with State-Space Constraint II" | SIAM J. Control Optim. 24(6):1110–1122, DOI `10.1137/0324067` | Comparison/uniqueness for state-constrained HJB; the state-constraint boundary condition inside the comparison class | PRIMARY THEOREM at reference level; project-specific hypothesis verification = PLAUSIBLE-BUT-NOT-YET-PROVED (the comparison block, Outcome B) |
| G. Barles, P. E. Souganidis (1991), "Convergence of approximation schemes for fully nonlinear second order equations" | Asymptotic Analysis 4(3):271–283, DOI `10.3233/ASY-1991-4305` | Monotone + stable + consistent (+ comparison) ⟹ locally uniform convergence; consistency is operator/test-function based | PRIMARY THEOREM at reference level (Theorem 2.1); the state-constraint boundary extension is PROJECT-SPECIFIC (this audit's mapping) |
| I. Capuzzo-Dolcetta, P.-L. Lions (1990), "Hamilton–Jacobi equations with state constraints" | Trans. Amer. Math. Soc. 318(2):643–683, DOI `10.1090/S0002-9947-1990-0951880-0` | Viscosity theory for HJ equations with state constraints; one-sided boundary condition, maximal-subsolution characterization, comparison | PRIMARY reference; exact theorem locations to be confirmed against the primary text |
| C. Berge (1963), "Topological Spaces" (maximum theorem) | Oliver & Boyd; also K. Border, "Fixed Point Theorems with Applications", Ch. 9 | Maximum theorem / compact-max argument used for H_proj USC/continuity on the effective region (local effective compactness) | STANDARD THEOREM; all hypotheses stated and verified from the frozen objective (monotone-scheme report §D.4) |
| M. Bardi, I. Capuzzo-Dolcetta (1997), "Optimal Control and Viscosity Solutions of Hamilton–Jacobi–Bellman Equations", Birkhäuser, Ch. IV | Book; Ch. IV: state-constrained problems, constrained Hamiltonian | Background for the constrained Hamiltonian as an auxiliary object | PRIMARY reference at chapter level; conventions to be confirmed against the primary text |

### Load-bearing status labels (Rev-6 discipline)

- **PRIMARY THEOREM SAYS THIS:** Soner Part I uses the minimization value v with `H_S(x,p) = sup{−b·p − f}` and characterizes the state-constrained value as viscosity subsolution in the open domain / supersolution on the closed domain (Definition 2.1 / Theorem 2.1, locations as verified by the Reviewer), under hypotheses including a compact control metric space and bounded drift/running cost.
- **PROJECT-SPECIFIC DERIVATION SAYS THIS:** the transform `V = −v` gives `G_S(x,−V,−p) = −F_proj(x,V,p)`, so the project orientation is "subsolution on closure / supersolution in interior"; the TRUE control domain is c > 0, l ≥ 0, d ∈ R (no static budget, no installed hard bounds, τ as wage wedge only); the Hamiltonian effective domain is parameter-generic (0 < γ_c ≤ 1 ⟹ finite ⟺ p_b > 0; γ_c > 1 ⟹ finite ⟺ p_b > 0 or (p_b = 0, p_a = 0) — the γ_c > 1 branch conditional on the parameter regime, NOT fixture-derived); the continuous local effective-compactness/coercivity lemma on K_p ⊂ {p_b ≥ η > 0} (parameter-generic) and the resulting H_proj joint continuity/USC and Lipschitz-in-p (maximum over a fixed compact control set / compact-maximum (Berge) argument); the DISCRETE smooth-test coercivity / optimizer-localization lemma (monotone-scheme report §D.5: exact Taylor identity with |R_m| ≤ (C_φ C_geom/m)|μ|₁ before compactness; per-coordinate coercivity with m₀ finite — χ₁ cancels in the d-dominance ratio; common compact K_alpha^disc for exact/ε-maximizers for all m ≥ m₀; finite-m discrete max attainment; uniform O(1/m) second moment through the actual max); the local-state same-candidate recovery constructions with the exact χ algebra on the true control domain (parameter-independent).
- **PLAUSIBLE BUT NOT YET PROVED (the single bounded Outcome-B block — renamed in Rev 6):** the UNBOUNDED-CONTROL NUMERICAL-SCHEME / STATE-CONSTRAINT CONVERGENCE-APPLICATION BLOCK — (i) global finite-m stability/existence of discrete solutions under the raw unbounded controls (the raw ‖g‖∞ is infinite; needs localization + barriers/invariant set); (ii) strict contraction / unique-fixed-point for production (per-candidate weight sum < 1 does not give a uniform modulus; a genuine uniform λ bound on the actually localized candidate set or an effective-domain treatment is required); (iii) constant-test/operator-finiteness diagnostic (Q·1 = 0; sup g = +∞ over raw c for 0 < γ_c ≤ 1; the Bellman operator is not globally finite on arbitrary bounded functions; economically relevant solutions may live in the p_b > 0 effective region; BS/Soner mapping needs an effective-domain/generalized treatment); (iv) the relevant-viscosity-gradient effective-domain restriction (do all gradients used by the state-constraint comparison/consistency stay in the finite effective domain and away from p_b = 0 as needed — value-monotonicity-in-b or Inada lower bound or effective-domain-formulated comparison, all requiring project-specific verification); (v) the mapping of the local effective compactification (continuous H_proj, p_b > 0) into the Soner/CDL comparison theorem and the exact theorem application; (vi) the constrained characterization `ρV ≤ H_T` at boundary points (coupled sublemma; not load-bearing for the transfer). These are NOT carried forward as established.

### What is proved in this audit (Rev-6 level)

1. **Trichotomy answer (Issue step C):** the raw Kuratowski admissible-set graph target is sufficient but **over-strong** — not necessary for the operator-consistency components of state-constraint HJB scheme convergence. Arguments: (i) the half-relaxed-limit machinery requires operator/test-function consistency, never drift-set graph convergence; (ii) the 1D toy discriminator falsifies necessity by construction (quantifier-corrected proof).
2. **Half-relaxed-limit algebra (quantifier-corrected):** ū subsolution w.r.t. H_lim (max retained; no per-candidate inference); u̲ supersolution in the interior; boundary subsolution on the closure w.r.t. H_proj for every effective-domain smooth-test gradient (p_b > 0): trivial restriction + USC + recovery, with the discrete-to-continuous Taylor passage through the Bellman max justified by the discrete localization lemma (§D.5).
3. **TRUE Hamiltonian effective domain (parameter-generic):** 0 < γ_c ≤ 1 ⟹ finite ⟺ p_b > 0; γ_c > 1 ⟹ finite ⟺ p_b > 0 or (p_b = 0, p_a = 0); +∞ on {p_b < 0} ∪ {p_b = 0, p_a ≠ 0} (and, for 0 < γ_c ≤ 1, also at p_b = p_a = 0) — derived term-by-term from the frozen utility specification (γ_c > 0, φ > 0, χ_1 > 0; log branch included; no numeric γ_c / φ authority).
4. **Continuous local effective compactness/coercivity (PROVED):** common compact effective optimizer set K_alpha(K_p) on compact gradient sets K_p ⊂ {p_b ≥ η > 0}, with FOC-derived c/l/d bounds; sup = max there; |μ| ≤ M.
5. **Hamiltonian regularity on the effective region (PROVED):** H_proj joint continuity/USC and Lipschitz in p; the uniform O(1/m) second-moment bound over the CONTINUOUS optimizer set K_alpha(K_p) — used as the lower anchor and regularity input, NOT as the discrete-max candidate set.
6. **Discrete smooth-test coercivity / optimizer-localization (PROVED — Rev 6, route A):** for every compact (N, K_p ⊂ {p_b ≥ η > 0}) and every smooth test with locally bounded Hessian, for all m ≥ m₀ all exact/ε-maximizers of the finite-m discrete Bellman objective lie in ONE common compact control set K_alpha^disc; the finite-m discrete max is ATTAINED; uniform O(1/m) second moment through the ACTUAL max; logical order: raw controls → discrete coercivity/localization → compact maximizing controls → uniform drift/rate bound → uniform second moment → max-level Taylor consistency (monotone-scheme report §D.5; exact-arithmetic checks in `%TEMP%\dlh5vh_rev6_discrete.py`).
7. **Monotonicity / order preservation (PROVED, candidate level):** nonnegative weights; M-matrix residual; no ρ ≥ Σq. Scheme-level stability/strict-contraction are NOT claimed (see block).
8. **Local-state same-candidate recovery (PROVED, true control domain):** strict controls α_m := α eventually; tangent controls by explicit constructions per family; coupled (ε, δ) at the double tangents with explicit interval conditions and strict margins; exact-arithmetic verified. Rates from μ(s_m, α_m) only; exact first moment; O(1/m) second moment.
9. **Composite boundary-operator bounds at the corners/faces (on the effective region):** H_T ≤ liminf H_R ≤ limsup H_R ≤ H_proj.
10. **Corner-cone containment under the frozen taxonomy:** REV ⊆ R, TDEP ⊆ R, TREA ⊆ R, sector generators in the regular region — 0 violations, m ≤ 25, W_max ∈ {8, 10, 12} (scratch `%TEMP%\dlh5vh_frozen_taxonomy2.py`).
11. **Issue-54 orientations (consumed, re-applied):** w_T ∉ REV (μ_a < 0), w_RT ∉ TDEP (μ_a > 0); the finite-m lattice obstruction does not damage the recovery bridge at the W-corners.
12. **Issue-55 sequence re-test:** `s_m = (0, i_t^m(0) − 2), μ = (0, 1)` with μ_W = 1: violates only the raw graph test; operator-consistent; boundary transfer closed for every effective-domain smooth-test gradient (p_b > 0).
13. **Toy discriminator:** YES with the full quantifier-corrected component proof; stability/contraction claims scoped to the toy's compact-control model.

### What remains unproved (explicitly, Rev 6)

1. **The Outcome-B block (Rev-6 formulation):** the **UNBOUNDED-CONTROL NUMERICAL-SCHEME / STATE-CONSTRAINT CONVERGENCE-APPLICATION BLOCK** — (i) global finite-m stability/existence of discrete solutions under raw unbounded controls; (ii) strict contraction / unique fixed point for production; (iii) constant-test/operator-finiteness diagnostic (Q·1 = 0; sup g = +∞ over raw c for 0 < γ_c ≤ 1; operator not globally finite on arbitrary bounded functions); (iv) the relevant-viscosity-gradient effective-domain restriction (NOT derivable from the classical FOC, which presumes V_b > 0); (v) whether the local effective compactification on {p_b > 0} suffices for the chosen state-constraint comparison theorem or an extension/generalization is required (Soner compact-control assumption mapping; do not cite Soner as directly applicable until resolved) and the exact Soner-II/CDL application; (vi) the constrained characterization ρV ≤ H_T at the boundary (coupled optional sublemma; not load-bearing for the transfer). No global uniform η is demanded a priori. Not established within this audit.
2. Monotonicity normalization/stability details of the future scheme beyond the candidate level (order-preservation holds by construction in the scaled form; the final scheme-design and fixed-point choices belong to the Outcome-B block / next gate).
3. The full convergence theorem u_m → V (composition of the above; CONDITIONAL on the Outcome-B block).

No copyrighted material is committed; all primary references appear as metadata/DOI + concise standard-content statements.

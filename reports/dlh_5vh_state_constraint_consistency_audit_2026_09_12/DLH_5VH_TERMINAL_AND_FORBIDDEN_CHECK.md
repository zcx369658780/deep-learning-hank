# DLH-5V-H — Terminal and Forbidden Check (Micro-Rev Rev 6)

**Issue:** #56 / DLH-5V-H — State-Constraint HJB Boundary-Consistency Target Audit (Route E)
**Micro-Rev Rev 6:** Reviewer comment `5643802722` — `DLH_5VH_OUTCOME_B_NOT_YET_ACCEPTED__DISCRETE_BELLMAN_COERCIVITY_AND_STABILITY_WITH_UNBOUNDED_CONTROLS_FIX_REQUIRED`; bounded same-Issue/same-branch revision on top of Rev 5 `82edcbb7d1438fc108a5c17605414efda627b535`.

---

## 1. Exactly one terminal (Rev 6)

`DLH_5VH_THEORY_AUDIT_PARTIAL__ONE_BOUNDED_BOUNDARY_CONSISTENCY_OR_COMPARISON_GAP_REMAINS`

**Verdict rationale (one paragraph):** Rev 6 fixes the discrete-layer coercivity/stability defect on the true unbounded control domain while preserving all valid Rev-1–5 science. (1) **Discrete smooth-test coercivity / optimizer-localization lemma (PROVED — route A, monotone-scheme report §D.5):** for every compact (N, K_p ⊂ {p_b ≥ η > 0}) and every smooth test φ with locally bounded Hessian, the finite-m discrete Bellman objective Ψ_m(α) = g + Σ q[φ(x_r)−φ(x_s)] satisfies the exact Taylor identity with |R_m(α)| ≤ (C_φ C_geom/m)|μ|₁ DERIVED BEFORE compactness; per-coordinate coercivity (c: u(c) − ηc dominates O(c/m); c → 0 excluded by u → −∞ (γ_c ≥ 1) or by the uniform strict-concavity gap below the FOC value (γ_c < 1); d: −ηχ_1d²/(2s̄) dominates O(d²/m) — with χ_1 CANCELLING in the dominance ratio so m₀ is finite for every χ_1 > 0; l: −(ω_min/(1+φ))|l|₁^{1+φ} dominates the linear drift/remainder for φ > 0); hence all exact/ε-maximizers lie in ONE common compact control set K_alpha^disc independent of m ≥ m₀, the finite-m discrete max is ATTAINED, and the uniform O(1/m) second moment passes THROUGH the actual max. Logical order corrected: raw controls → discrete coercivity/localization → compact maximizing controls → uniform drift/rate bound → uniform second moment → max-level Taylor consistency. The continuous-Hamiltonian K_alpha(K_p) is used only as the uniform lower anchor (α* ∈ K_alpha(K_p) exists) and for H_proj regularity. (2) **D.1 fixed:** `‖u_m‖∞ ≤ ‖g‖∞/ρ` is INVALID on the raw domain (‖g‖∞ = ∞) — DELETED; per-candidate weight sum < 1 does not give a global strict-contraction modulus (λ unbounded on raw controls) — DEMOTED; monotonicity/order preservation remains PROVED at the candidate level. (3) **Constant-test / operator-finiteness diagnostic recorded:** Q·1 = 0; sup over raw c of g is +∞ for 0 < γ_c ≤ 1 ⟹ the Bellman operator is not globally finite on arbitrary bounded functions; economically relevant solutions may live in the p_b > 0 effective region; BS/Soner mapping needs an effective-domain/generalized treatment — recorded inside the block. (4) **Outcome-B block renamed** to the UNBOUNDED-CONTROL NUMERICAL-SCHEME / STATE-CONSTRAINT CONVERGENCE-APPLICATION BLOCK: (i) global finite-m stability/existence of discrete solutions, (ii) strict contraction / unique fixed point for production, (iii) constant-test/operator-finiteness diagnostic, (iv) relevant-viscosity-gradient effective-domain restriction (all gradients used by the state-constraint comparison/consistency stay in the finite effective domain, away from p_b = 0 as needed; no universal η demanded), (v) local-effective-compactification → Soner/CDL comparison mapping and exact theorem application, (vi) optional constrained characterization ρV ≤ H_T. (5) **Boundary transfer:** PROVED for every effective-domain smooth-test gradient (p_b > 0, locally bounded Hessian) via the D.5 localization + trivial restriction + USC + recovery; overall CONDITIONAL_ON_EFFECTIVE_DOMAIN_REGULARITY. (6) **Outcome C NOT triggered:** the discrete coercivity lemma shows the frozen process CAN define a finite legitimate discrete Bellman score on the p_b > 0 smooth-test class (sup attained on the localized set, bounded below uniformly by the continuous anchor); the unresolved material is the general unbounded-control scheme/comparison theory, which is not grounds for Outcome C. Outcome A is not authorized by the current evidence. **Outcome B.**

## 2. Forbidden-operation check

| Forbidden operation | Status |
|---|---|
| Household / economic / D_W mutation | NOT performed (blob re-verified: `76ae5b149993a7edeeb8eb337f1b02b3fe33c51e`; χ/μ formulas re-read from source lines 80–157) |
| Promote validation-fixture parameters into theory authority | NOT performed — γ_c = 2 / φ = 5 claims removed; the fixture instances are cited only as `VALIDATION_FIXTURE_NOT_CALIBRATION`, not as theory authority |
| Turn μ_b ≥ 0 into a global constraint | NOT performed — the Rev-3 static-budget Γ is REMOVED |
| Add hard l/d bounds not in accepted authority | NOT performed — l_max/d_min/d_max not introduced as feasibility; bounds only derived effectively from the frozen objective |
| Grid / aspect / state-family redesign, state augmentation, coordinate transformation | NOT performed (frozen geometry consumed) |
| Numerical production W_max | NOT used (W_max symbolic; W_max ∈ {8, 10, 12} only in scratch enumeration) |
| Implementation / solver-source mutation | NOT performed (no source files touched) |
| Production-Q assembly/run, HJB/KFE/stationary solve | NOT performed |
| KFE-only repair, pin/normalization leakage repair | NOT performed |
| Aggregates / GE / multi-region / neural / nominal / calibration / policy / welfare / Results | NOT performed |
| PR / merge / close / successor / self-accept | NOT performed (same branch amended; STOP for fresh ChatGPT review) |
| Stationary KFE | NOT AUTHORIZED, NOT performed |
| Copyrighted theory PDFs / long quotations in repo | NOT committed (metadata/DOI + concise standard-content statements only) |
| Silent revision of Issue #54 / #55 accepted verdicts | NOT performed (both consumed; the necessity question is answered, not their verdicts revised) |
| New Issue / new branch / branch reset / discard of Rev 0–5 candidates | NOT performed (Rev-6 commit is on top of `82edcbb` on the SAME branch) |

## 3. Rev-6 deliverables checklist (Reviewer comment `5643802722`)

| Reviewer item | Status |
|---|---|
| 1. Continuous optimizer compactness does NOT justify the discrete Bellman max remainder — prove a DISCRETE coercivity / optimizer-localization lemma (route A) or demote the max-level claim | DONE (route A) — §D.5 lemma PROVED: exact Taylor identity with \|R_m(α)\| ≤ (C_φ C_geom/m)·\|μ\|₁ derived BEFORE compactness; per-coordinate coercivity (c, c→0, d with χ₁-cancelling m₀, l with (1+φ)-power); common compact K_alpha^disc for all exact/ε-maximizers for all m ≥ m₀; finite-m discrete max ATTAINED; logical order corrected (continuous K_alpha = lower anchor only) |
| 2. D.1 `‖u_m‖∞ ≤ ‖g‖∞/ρ` invalid on the raw domain | DONE — DELETED; ‖g‖∞ = ∞ on the raw control set (g unbounded below in l for every φ > 0, unbounded above in c for 0 < γ_c ≤ 1); monotonicity/order preservation kept as PROVED at candidate level; scheme-level stability DEMOTED to the block |
| 3. Strict contraction not implied by per-candidate weight sum < 1 | DONE — DEMOTED: λ unbounded on raw controls ⟹ sup λ/(ρ+λ) can approach 1; a uniform contraction modulus requires a uniform λ bound on the actually localized set or an effective-domain treatment (block item) |
| 4. `max` / ONE-argmax / ONE-Q requires discrete attainment | DONE — §D.5 proves attainment of the finite-m discrete smooth-test score on K_alpha^disc for m ≥ m₀ (nonempty argmax set); ONE-selection is the fixed tie-break convention (uniqueness NOT claimed); scheme-level argmax outside the smooth-test class NOT claimed (block item) |
| 5. Uniform second moment — correct logical order | DONE — raw controls → discrete coercivity/localization → compact maximizing controls → uniform drift/rate bound → uniform O(1/m) second moment → max-level Taylor consistency; the continuous K_alpha bound is used only as lower anchor + regularity input |
| 6. Constant-test / γ_c ≤ 1 diagnostic recorded in the block | DONE — §D.6: Q·1 = 0; sup over raw c of g is +∞ for 0 < γ_c ≤ 1; the Bellman operator is not globally finite on arbitrary bounded/constant functions; economically relevant solution may live in the p_b > 0 effective region; global bounded-function contraction cannot be assumed; BS/Soner mapping needs effective-domain/generalized treatment |
| 7. Boundary / half-relaxed-limit status | DONE — the continuous restriction H_m ≤ H_proj and H_proj USC at fixed p_b > 0 remain valid; the FULL discrete-to-continuous Taylor passage through the Bellman max is PROVED via the §D.5 localization; boundary smooth-test operator consistency PROVED locally for p_b > 0; the transfer is PROVED for every effective-domain smooth-test gradient and overall CONDITIONAL_ON_EFFECTIVE_DOMAIN_REGULARITY (no conflation of continuous H_proj USC with finite-m max consistency) |
| 8. Outcome-B packaging / Outcome-C check | DONE — the single bounded block is renamed the UNBOUNDED-CONTROL NUMERICAL-SCHEME / STATE-CONSTRAINT CONVERGENCE-APPLICATION BLOCK (items: scheme-level stability/existence, strict contraction for production, constant-test finiteness, relevant-gradient effective-domain restriction, Soner/CDL mapping + exact application, optional H_T characterization); Outcome C NOT triggered (the frozen process defines a finite legitimate discrete score on the p_b > 0 smooth-test class) |
| 9. Preserve Rev-1–Rev-5 valid repairs | DONE — all preserved (Soner sign transform, H_T downgrade, parameter-generic continuous effective-domain theorem, continuous local effective compactness, local-vs-global η, exact-χ ledger, taxonomy, μ_W = 1, quantifier fix, mirror wording, true control domain, τ correction, no hard bounds, trivial restriction) |

## 4. PROVED / UNRESOLVED ledger (matches the evidence)

**PROVED:** Soner sign mapping (explicit transform); raw-graph non-necessity mechanism (BS operator structure + corrected toy + corrected half-relaxed-limit algebra — never via the unresolved block); monotonicity / order preservation at the candidate level (M-matrix residual, nonnegative weights, no ρ ≥ Σq); interior consistency; stencil destination availability; finite-m rate algebra conditional on a locally admissible candidate; local-state same-candidate recovery (ALL strict and ALL tangent controls at every family, exact χ algebra, TRUE control domain, parameter-independent); TRUE Hamiltonian effective domain (parameter-generic for every frozen γ_c > 0, φ > 0, log branch included); continuous local effective compactness/coercivity on compact K_p ⊂ {p_b ≥ η > 0} (P^{−1/γ_c} ≤ c* ≤ η^{−1/γ_c}, l/d bounds, sup = max, |μ| ≤ M — no numeric γ_c / φ value); H_proj joint continuity/USC and Lipschitz in p on the effective region (maximum over a fixed compact control set / compact-maximum (Berge) argument); **DISCRETE smooth-test coercivity / optimizer-localization lemma (§D.5): common compact K_alpha^disc for exact/ε-maximizers for all m ≥ m₀, finite-m discrete max ATTAINED, uniform O(1/m) second moment through the ACTUAL max (route A)**; boundary smooth-test operator consistency on the closure w.r.t. H_proj for every effective-domain test gradient (p_b > 0; D.5 localization + trivial restriction + USC + recovery); interior supersolution; composite H_T ≤ liminf H_R ≤ limsup H_R ≤ H_proj on the effective region.

**UNRESOLVED (the single bounded Outcome-B block — renamed):** the UNBOUNDED-CONTROL NUMERICAL-SCHEME / STATE-CONSTRAINT CONVERGENCE-APPLICATION BLOCK — (i) global finite-m stability/existence of discrete solutions under the raw unbounded controls (the raw ‖g‖∞ is infinite; needs the discrete localization plus valid sub/supersolution barriers or a bounded fixed-point invariant set); (ii) strict contraction / unique fixed point for production (per-candidate weight sum < 1 does not give a uniform modulus; a genuine uniform λ bound on the actually localized candidate set or an effective-domain treatment is required); (iii) constant-test / operator-finiteness diagnostic (Q·1 = 0; sup g = +∞ over raw c for 0 < γ_c ≤ 1; the Bellman operator is not globally finite on arbitrary bounded functions; economically relevant solutions may live in the p_b > 0 effective region; BS/Soner mapping needs an effective-domain/generalized treatment); (iv) whether all viscosity gradients actually used by the state-constraint comparison/consistency argument remain inside the finite effective Hamiltonian domain, and where needed away from p_b = 0 (routes: value-monotonicity-in-b, Inada lower bound, effective-domain-formulated comparison — none established in scope; the classical FOC presumes V_b > 0); (v) whether the local effective compactification on {p_b > 0} is sufficient for the chosen state-constraint comparison theorem, or an extension/generalization is required (Soner Part I assumes a compact control space and bounded data; do not cite Soner as directly applicable until resolved), and the exact Soner-II / Capuzzo-Dolcetta–Lions application; (vi) constrained characterization ρV ≤ H_T at the boundary (coupled optional sublemma of the same block; not needed for the transfer).

## 5. Fresh-state evidence (at candidate time)

- Fresh live `main`: `5d38c644ec1a06359241f9c3418dcd815c938ec6` (fresh fetch; unchanged).
- Issue #56: OPEN; task type `SCIENTIFIC_THEORY_AUDIT__STATE_CONSTRAINT_HJB_BOUNDARY_CONSISTENCY_TARGET`; activation comments `5641527846`, `5641534701`; Reviewer comments `5641997550`, `5642335215`, `5642677284`, `5642868947`, `5643574257`, `5643802722`; Rev-5 completion `5643749417`; authority marker `DLH_5VH_ROUTE_E_STATE_CONSTRAINT_HJB_CONSISTENCY_TARGET_AUDIT_AUTHORIZED`.
- Previous candidates: `bc281a693b2d9281b6e180bc62996d503dfab7fe` (Rev 0), `5d1e4822dcc94aa16d30837e9c524baaefe5fa3d` (Rev 1), `52cfbf4cb4a1bcdb36bd77211869e8ac0f51b317` (Rev 2), `dd3aa425a869a2d5a09835efc667c4ff1c2bb5b3` (Rev 3), `e8d49fe9d02fbb5961bc1bf7076a4b2d93e767fb` (Rev 4), `82edcbb7d1438fc108a5c17605414efda627b535` (Rev 5; kept; Rev 6 on top).
- Household blob: `76ae5b149993a7edeeb8eb337f1b02b3fe33c51e` ✓ (χ/μ formulas re-verified from source lines 80–157; parameters validated: χ_0 ≥ 0, χ_1 > 0, a_bar > 0, γ_c > 0, φ > 0 — positivity only; γ_c = 2 / φ = 5 are `VALIDATION_FIXTURE_NOT_CALIBRATION` and are not theory authority).
- Only the six allowlist paths modified; staged explicitly; remote SHA = local SHA verified after push.
- Exact-arithmetic scratch (TEMP only): `%TEMP%\dlh5vh_rev6_discrete.py` — REV/TDEP/TREA second moments = O(\|μ\|₁/m) with C_geom ≈ 7.37 (matches the accepted Case-L formula `[2660μ_a + 133(−μ_W)]/(361m)`); χ₁ cancels in the d-dominance ratio; m₀ finite (c-coordinate ≈ 736, d-coordinate ≈ 3·10⁴ with conservative constants; existence, not tightness); l-coordinate needs no m₀ (superlinear (1+φ)-power).

## 6. Companion reports (this package)

- `DLH_5VH_AUTHORITY_AND_PRIMARY_THEORY_CAPSULE.md` (B: provenance, sign mapping, H_T downgrade, status labels, Rev-4 ledger)
- `DLH_5VH_CONTINUOUS_STATE_CONSTRAINT_HJB_AND_VISCOSITY_TARGET.md` (A: continuous target, Soner transform, H_T auxiliary, true control domain + effective-domain audit §3.5)
- `DLH_5VH_STRONG_GRAPH_TARGET_NECESSITY_AUDIT.md` (C: trichotomy — over-strong; operator-level restatement on the true domain; raw-graph scoping)
- `DLH_5VH_MONOTONE_SCHEME_BOUNDARY_CONSISTENCY_AND_FROZEN_PROCESS_TEST.md` (D+E+F: corrected quantifier, exact-χ recovery ledger on the true domain, effective-domain regularity, discrete smooth-test coercivity/localization §D.5, stability demotion §D.1, constant-test diagnostic §D.6, toy)
- `docs/theory/DLH_5VH_STATE_CONSTRAINT_HJB_BOUNDARY_CONSISTENCY_TARGET_AUDIT.md` (umbrella)
